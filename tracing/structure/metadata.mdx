---
title: Metadata
description:
---

Trace metadata applies to the entire trace. Use it for information that's constant across spans:

- **Environment** — production, staging, development
- **Region** — us-west, eu-central
- **Deployment** — version numbers, feature flags, A/B test variants
- **Request context** — correlation IDs, upstream service info

Set metadata early—ideally where the trace begins—so all spans share the same context.

<Tabs items={['TypeScript', 'Python']}>
  <Tab title="TypeScript">
    ```typescript
    Laminar.setTraceMetadata({ environment: 'production', feature: 'chat' });
    ```
  </Tab>
  <Tab title="Python">
    ```python
    Laminar.set_trace_metadata({ "environment": "production", "feature": "chat" })
    ```
  </Tab>
</Tabs>

See also: [Trace methods (TypeScript)](/sdk/trace-methods#laminar-settracemetadata-metadata) • [Trace methods (Python)](/sdk/trace-methods#laminar-set_trace_metadata-metadata)
